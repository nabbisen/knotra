# Context Operations

Switch working branch (Git) or change-set (jj) across projects.

**Workflow**: select project → browse candidates (search-filtered) → click target → confirm dialog (dirty-tree warning shown) → result screen with commands executed and recovery hint on failure.

**VCS note**: Git uses `git switch`; jj uses `jj edit`. The UI presents both as "context switch".
